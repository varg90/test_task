language: ruby
rvm:
  - 2.6.3
script:
  - bundle exec rake db:drop db:create db:schema:load RAILS_ENV=test
  - bundle exec rspec
cache:
  yarn: true
  bundler: true
  directories:
    - node_modules
services:
  - postgresql
addons:
  postgresql: "9.4"
